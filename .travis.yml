sudo: false
language: node_js
node_js:
  - "lts/*"
  - "8"
matrix:
  include:
    - node_js: "lts/*"
      env: TEST_SUITE=format:ci
    # Don't worry if coverage tests to fail for now.
    # - node_js: "lts/*"
    #   env: TEST_SUITE=coverage
env:
  - TEST_SUITE=unit
script: npm run-script $TEST_SUITE
